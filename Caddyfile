beta.shapefyapp.com {
    root * /usr/share/caddy
    encode gzip

    # Proxy /api para o backend
    reverse_proxy /api/* https://beta.shapefy.online {
        header_up Host beta.shapefy.online
    }

    # SPA: redireciona tudo para index.html
    file_server
    try_files {path} /index.html
}
